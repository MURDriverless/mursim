#include "wheel.hpp"

namespace mursim
{
    Wheel::Wheel(gazebo::physics::ModelPtr &model,
            sdf::ElementPtr &sdf,
            std::string name,
            gazebo::transport::NodePtr &gznode,
            std::shared_ptr<ros::NodeHandle> &nh_ptr)
    {
        // LOAD WHEEL PARAMS
    }
}